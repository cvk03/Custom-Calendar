<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Layout Properties -->
    <size
        android:width="300dp"
        android:height="42dp" />

    <!-- UI Properties -->
    <solid
        android:color="@color/white"/>
    <corners
        android:radius="20dp" />
    <stroke
        android:width="1dp"
        android:color="#D5D6DD" />
</shape>